// snippet with prefix: rnfc
import React from 'react';
import { StyleSheet, View } from 'react-native';

function Testing(props) {
   return (
     <View style={styles.container}></View>
    );
}

const styles = StyleSheet.create({
    container: {
    }
})
export default Testing;